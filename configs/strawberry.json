{
    "_comment_species": "当前处理的水果类型名称，用于在一些日志和输出中区分",
    "species": "strawberry",
    "_comment_data_dir": "纯点云数据（包含 complete 和可选的 partial 文件夹）的存储根目录",
    "data_dir": "/home/tianqi/corepp2/data/20260331_dataset/",
    "_comment_checkpoint_dir": "Encoder 模型权重(checkpoints)在训练期间保存的路径",
    "checkpoint_dir": "./logs/strawberry/checkpoints/",
    "_comment_checkpoint_file": "最佳模型保存的自定义命名后缀",
    "checkpoint_file": "_strawberry_best_model.pt",
    "_comment_log_dir": "供 TensorBoard 读取的训练日志和曲线的保存路径",
    "log_dir": "./logs/strawberry/runs/",
    "_comment_encoder": "指定使用的网络结构。这里我们因为是纯点云，必须用点云网络：foldnet、point_cloud 或 point_cloud_large",
    "encoder": "pointnext",
    "_comment_detection_input": "对纯点云任务不生效(为图像任务保留的占位符)",
    "detection_input": "mask",
    "_comment_normalize_depth": "对纯点云任务不生效(为深度图任务保留的占位符)",
    "normalize_depth": false,
    "depth_min": 230,
    "depth_max": 350,
    "_comment_batch_size": "小批量训练的 Batch 大小",
    "batch_size": 4,
    "_comment_input_size": "输入 DataLoader 的点云采样数量(即降采样/补全后的固定点数)",
    "input_size": 2048,
    "_comment_epoch": "网络训练的总轮数",
    "epoch": 100,
    "_comment_lr": "初始学习率",
    "lr": 0.0001,
    "_comment_loss_weights": "各类高级损失函数的权重占比，在目前基础的点云Latent拟合任务中用不上",
    "lambda_kl": 100,
    "lambda_attraction": 1,
    "lambda_repulsion": 1,
    "lambda_rendering": 1,
    "_comment_loss_type": "选择 SDF 损失函数：original (默认 L1) 或 weighted (新加加权)",
    "loss_type": "weighted",
    "sdf_alpha": 15.0,
    "lambda_sdf": 1,
    "_comment_intervals": "保存和验证的频率(代表每训练多少个Epoch执行一次)",
    "checkpoint_frequency": 10,
    "validation_frequency": 10,
    "_comment_supervised_3d": "【非常关键】是否开启3D监督，即通过计算MSE损耗对比 Encoder输出 和 预训练DeepSDF(真实Latent Code)的差距进行优化。点云流程必须为 true",
    "supervised_3d": true,
    "_comment_switches": "开关控制各种附加结构的约束功能，目前对单一生成 Latent 目标的骨干任务都可以关掉",
    "kl_divergence": false,
    "contrastive": false,
    "rendering": false,
    "reg_latent": false,
    "3D_loss": true,
    "_comment_threshold": "验证阶段计算匹配距离(如Chamfer Distance)的一些内置阈值",
    "threshold": 0.0007,
    "_comment_grid_density": "SDF渲染时使用的网格分辨率基准 128",
    "grid_density": 30,
    "_comment_normalization_scale": "将网络局部空间还原至物理世界空间的缩放比例(由预处理时决定)，体积对齐以此值为基准。如果是按全数据集计算的统一缩放比则填入对应实数，如果不缩放则填 1.0",
    "normalization_scale": 45.54,
    "_comment_lambda_volume": "体积监督损失权重，用于直接约束预测体积不要塌缩到均值附近；重训时提高到更强。",
    "lambda_volume": 0.5,
    "_comment_lambda_latent_spread": "潜变量分布展开损失权重，用于抑制 encoder 输出 latent collapse；重训时提高到更强。",
    "lambda_latent_spread": 1.0,
    "_comment_calibrate_volume_head_on_val": "测试时是否使用验证集对 volume head 做线性校准。默认关闭，直接以 raw head 作为主结果更稳。",
    "calibrate_volume_head_on_val": false,
    "_comment_lambda_super": "Latent MSE 主监督权重。为避免继续把预测 latent 拉回均值附近，这里显式下调。",
    "lambda_super": 0.3,
    "_comment_volume_head_calibration_degree": "保留兼容字段；当前测试逻辑默认只支持线性校准，建议维持 1 或忽略该项。",
    "volume_head_calibration_degree": 1,
    "_comment_volume_loss_relative_weight": "体积损失中相对误差项的权重。数值越高，训练越关注小果和大果的比例误差",
    "volume_loss_relative_weight": 0.5
}